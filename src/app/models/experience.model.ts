export interface Experience {
  title: string;
  company: string;
  period: string;
  responsibilities: string[];
  tags: string[];
}

export interface Project {
  title: string;
  description: string;
  tags: string[];
}